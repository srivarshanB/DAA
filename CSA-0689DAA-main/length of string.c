#include<stdio.h>
#include<string.h>
int main()
{
	int i,j,s,length;
	char str1[20];
	printf("enter the given value:");
	scanf("%s",&str1);
	length=strlen(str1);
	printf("length of the string %d:",length);
	return 0;
}
